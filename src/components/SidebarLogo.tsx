"use client";

import React from "react";

export default function SidebarLogo() {

  return (
    <div className="text-center group cursor-default">
      <h1 className="text-xl font-black tracking-tighter text-[var(--positive)] transition-all group-hover:tracking-widest">
        NINE SIX
      </h1>
      <p className="text-[10px] text-[#555] uppercase tracking-[0.2em] font-bold">Labs</p>
    </div>
  );
}
